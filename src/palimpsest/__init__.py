"""palimpsest -- a version-control system for Illinois statutory law.

This package currently implements **Oracle-0** (spec §4.2): the single-hop
self-check that measures how often the compiled ILCS text of a section matches
the text of that same section as reprinted by the Public Act its own
``(Source: ...)`` line says last set it.
"""

__version__ = "0.1.0"
